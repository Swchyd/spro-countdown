{
  "name": "spro-library-gatekeeper",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Authorization gatekeeper for the SPro Countdown song library",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node test/gatekeeper.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.120.0"
  }
}
